<div class="card">
    <div class="card-header">
        {{title}}
    </div>
    <div class="card-body">
        <div class="content">
            {{content}}
        </div>
        <div class="user-info">
            <br>
            - {{user.username}} | {{format_date created_date}}
        </div>
    </div>
</div>