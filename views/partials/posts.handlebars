<div class="card">
    <div class="card-header">
        <p class="">{{title}} </p>
    </div>
    <div class="card-body">
        <div class="content">
            {{content}}
        </div>
        <div class="user-info">
            <br>
            - {{user.username}} | {{format_date created_date}}
        </div>
    </div>
</div>